# 重要决定记录

按时间排列。每条:决定 + 理由。细粒度设计参数见 `.hallmark/log.json`,完整过程见 git log。

## 2026-07-22 · 建站与方向摸索(v1–v5)

- **网站定位**:面向 AI/增长方向产品岗的招聘方,目标是"一分钟建立'能落地的产品实习生'印象"。
- **诚实红线(始终有效)**:所有数字仅用简历真实数据(+70%/10w+/+30%/CPC-30% 等);LibLib 段在补齐前如实标"进行中";杜绝一切编造指标。
- **案例浮层机制**:四段实习各配一个可点开的案例详情(问题→方案→结果),内嵌按各公司产品 UI 气质重绘的示意图;因在职合规,**不用真实截图、不用内部数据**,每个浮层带「示意重绘」标注,示意图数据用色条代替文字。
- 主题走了 Studio 纸面编辑风 → Lumen 暗夜工坊 → 复古工程档案 → 磷绿 CRT 像素五轮迭代;用户反复反馈核心是"太 AI 化/太模板化"。

## 2026-08-01 · Kimi DNA 重构(v6)

- **用户指定模仿 careers.kimi.com**。经浏览器渲染提取 DNA:纯黑白、中文像素字单字体、可交互像素装置、游戏机气质。据此全站重构:黑白 + 极少量绿(用户选定,零辉光)。
- **字体方案**:缝合像素字体(Fusion Pixel 12px Mono zh_hans,OFL)按全站用字子集化到 ~22KB 自托管——不依赖 Google Fonts(国内可达性);**改文案必须重新子集化**(缺字会混入系统黑体,视觉上像乱码)。
- **动效移植三件套**:hover 光线(Kimi 原始缓动参数)、像素溶解转场、pulse 呼吸;**刻意不搬**滚轮劫持叙事舞台(HR 停留时间短,信息必须直达)。

## 2026-08-01 · 视觉去模板化(v7,现行版)

- **诊断共识**:AI 感来源于"营销页骨架"(hero→数据条→卡片格),而非配色字体。解法是给页面一个"视觉世界"。
- **两方向原型比稿后用户选定「像素 RPG 存档界面」**:HUD 顶栏 / 角色面板+装备栏 / 世界地图关卡(点击开案例浮层)/ EXP 条 / STAGE 5 未解锁收尾。弃选:像素海报编辑排版。
- **工程教训(原型翻车复盘)**:①场景布局必须用有界画布(1200px 居中),否则宽屏散架;②字体子集必须覆盖最终全部文案;③验证必须跑满 375/768/1280/1440/1920 五档宽度。
- **整屏翻页**:每板块占满一个视口。scroll-snap 打底 + 滚轮 rAF 手动缓动(700ms cubic);两个关键坑:`scroll-behavior:smooth` 会毁掉逐帧动画、mandatory 吸附会取消 smooth scrollTo。宽 <1024px 或高 <704px 自动降级为自然滚动(移动端内容完整性优先)。
- **文案口径**:卡片标题用专业名词短语(用户要求去口语化);"医生端"改"产品改版";教育背景不显示时间;LibLib 职位为"海外增长策略实习生"(2026.5–2026.8)。

## 2026-08-01 · 上线决定

- **部署到新建仓库 `amuu-2.github.io`(根域名)**,不覆盖旧 personal-web 仓库;已 live 验证(200)。
- **隐私**:手机号 19519875185 + 邮箱公开——用户明确确认。
- **发布卫生**:7 个试衣间/原型草稿页从发布分支删除(git 历史保留);main=发布、kimi-pixel=开发。
- **待办**:简历 docx 里的旧链接(amuu-2.github.io/personal-web)待更新为新地址;用户手机真机自测;后续候选方向见 CLAUDE.md。

## 2026-08-11 · CRT 开场动画(intro/)

- **接入 Blender 预渲染的 CRT 开场**:昏暗房间待机循环 → 点击电脑 → 推镜(1.5s,带运动模糊)→ 打字机开机自检 → 飞进屏幕露出正文。技术路线是"预渲染视频 + DOM 接管",不用 Three.js;源项目(Blender 场景脚本/渲染管线/踩坑记录)在 `Desktop\intor\files`,仓库只放成品(两段 mp4 共 215KB)。
- **每次访问都播**(`onlyFirstVisit:false`)。曾默认"只对新访客播一次",上线后用户改为每次都播,理由:不想看的人点右下角跳过即可,不该替访客决定。`?intro=skip` 跳过 / `?intro=debug` 校准调参面板。尊重 `prefers-reduced-motion`(无障碍要求,仍直接跳过)。
- **与站点的耦合刻意压到两点**:`window.__introActive` 旗标(站点滚轮翻页与首屏 dissolve 据此让位)+ 结束时调站点的 `pixelDissolve` 交接。开场层从 DOM 自拆,正文始终是真实 DOM(SEO/读屏不受影响)。
- **屏幕内联动(2026-08-11 追加)**:打字结束后,CRT 屏幕里亮起网站首屏的缩影(同源 iframe 实时渲染,非截图,永不过时),随飞入同步放大,结束时与真实视口 1:1 像素对齐(iframe 按 contain 系数 k0 缩进屏幕矩形,飞入放大系数恒等于 1/k0)。自然到达不再放 dissolve(已无缝);跳过/Esc 仍放 dissolve 补进场。
- **清晰化走像素语言,不走高斯模糊**(用户明确否决 blur 滤镜):SVG 马赛克滤镜(feFlood+feTile+feMorphology)+ 色阶量化(呼应抖动 levels:6,仅粗块阶段启用,细块阶段关掉否则像脏色带),飞入时逐档升分辨率 6→5→4→3→2→1→清晰,亮度同步"逐档通电";硬边跳档,和站点转场的 steps 同一语言。

### 2026-08-11 · 修复:案例浮层在手机上显示不全(定位参照系错了)

- **症状**(用户手机实拍):在第二屏世界地图点击实习关卡,弹出的案例浮层只显示成屏幕顶部的一条窄白条,内容看不全。
- **根因**:`.case` 是 `position: relative`,浏览器给 `<dialog>` 的默认定位是 **`absolute`** —— 参照系是**整个文档**而不是视口。所以浮层永远被钉在文档顶部附近(y≈20~792)。用户滚到第二屏(scrollY≈900)点击时,浮层其实在屏幕上方之外,只露出最底下一条。
- **为什么桌面没暴露**:桌面浏览器会自动把模态框滚进视野(实测点击后 scrollY 被从 812 拽回 0),掩盖了问题;手机上这个自动滚动没生效。站点 JS 里那段"打开时记住滚动位置、关闭后恢复"的 `returnY` 逻辑,就是当初为了对付这个跳屏而写的补丁 —— 治标没治本。
- **修复**:`position: fixed; inset: 0; margin: auto; height: fit-content` —— 相对视口居中,滚到哪都完整可见,且**不再跳屏**(`returnY` 逻辑保留,现在是无害的兜底)。
- **验证**:375/320/1440 三档 × 第1/2/3屏三个滚动位置 × 四个案例浮层 × 三条关闭路径(按钮/点背景/Esc),全部完整可见、零跳屏、关闭后停在原位。

### 2026-08-11 · 手机端场景化重构(Hallmark redesign · mobile-only)

- **诊断**:桌面版的设计骨架是"整屏翻页 = 一屏一个场景"(RPG 存档界面的核心隐喻)。手机上翻页降级为自然滚动后,板块高度变成内容驱动的随机值(实测 **0.94 / 1.25 / 0.61 屏**),场景感消失,变成一个普通长网页 —— 这就是用户说的"电脑端硬搬 / 填不满 / 不和谐"。
- **解法:恢复场景节奏,但不劫持滚动**。不把 scroll-snap 加回手机(会和原生滚动打架,且移动端内容完整性优先);改为每个板块 `min-height: calc(100dvh - hud)` + 内容垂直居中。用户照常自然滚动,但每一屏都落成完整场景。结果 **0.93 / 0.93 / 0.93 屏**。
- **三场景按竖屏重新构图**:
  - 角色面板 → **居中角色卡**。原来立绘 58% 宽左对齐,右侧 43% 死白(桌面两栏塌成一栏的残留);现改立绘居中、名字/职业居中、装备栏由 flex-wrap 改 **grid 三列**铺满(flex 末行会留不齐的缺口)。触屏无 hover,装备提示 `display:none` 不占位。
  - 世界地图 → **视口驱动**。原画布写死 960px 与视口无关(1.25 屏的尴尬高度);改 `min-height: max(calc(100dvh - hud - exp), 34rem)`,关卡改百分比定位,旗杆/路牌按手机缩小。下限 34rem 是为 320×568 这类矮屏兜底 —— 宁可地图略超一屏,也不能让关卡被 `overflow:hidden` 裁掉。
  - 收尾 → 撑满一屏、垂直居中(原 0.61 屏)。
- **两个实现坑(已写进注释)**:
  1. **不要给 `.world` 加 `display:flex`** —— `.world__canvas` 带 `margin-inline:auto`,在 column flex 容器里交叉轴两侧 auto 边距会取消 stretch,画布塌成 0 宽,五个关卡全挤到正中、路牌被压成 65px 竖条。直接给画布设高即可。
  2. **路牌宽度必须跟视口收**(`min(150px, 40vw)`)。固定 150px 在 320 宽屏上会让左右两列水平相撞 —— 竖版走位靠的就是"左右两列不交叠",所以纵向间隔小于关卡高度也不会重叠。
- **验证**:320 / 375 / 414 / 768 四个宽度全过(无横滚、关卡零重叠、无裁切、立绘居中);桌面 1440 回归两栏 + 横版地图 + 整屏翻页,未被误伤。

### 2026-08-11 · 移动端适配

- **问题实测**:手机竖屏(375×812)下 16:9 的开场画面只占视口 **26%** 高,上下各 301px 黑边,开机文字掉到 **4px** 不可读。
- **解法:舞台比例自适应 + 横向居中裁切**。`stageAspect = clamp(视口比, 0.9, 16/9)`,竖屏收窄到 0.9;裁到 0.9 时仍完整保留显示器(x 39.6~50.2%)与百叶窗(x 41~71%),再方就要切掉窗户。改造后竖屏占视口 **51%**、开机文字 **16.9px**、点击热区从 71×59 变 141×117。
- **裁切必须做在 WebGL 采样坐标上**(着色器加 uvScale/uvOffset;抖动格子留在屏幕空间,只映射采样点)。画面是 canvas 画的,CSS 的 `object-fit` 管不到它,直接改 CSS 只会把画面拉扁。2D 降级路径用 drawImage 的源矩形做同样裁切。
- 所有矩形经 `mapRect()` 统一换算到裁切后坐标系,**落地无缝的 scale=1/k0 关系因此自动成立**(两边用的是同一个映射后矩形)。
- 顺带:笔记本 16:10 从"上下黑边"变成填满视口(裁 10%),16:9 与带鱼屏行为不变。触控目标补到 ≥44px(HUD 联系我、案例浮层关闭按钮)。
- **调试陷阱记录**:浏览器面板隐藏时 CSS 动画/过渡冻结在起始帧,`getBoundingClientRect` 会读到错值(跳过按钮 opacity 恒 0、案例浮层恒 scale(0.97) 导致 44px 按钮量出 42.68px)。判断方法:临时禁掉动画/过渡再量,或对比 computed style 的目标值。

### 2026-08-11 · 预览方案定案:实时 iframe(否决抖动截取)

试装了三种屏幕内像素化方案并实测对比:**a** 实时 iframe + SVG 马赛克逐档、**b** 实时截取首屏 DOM(SVG foreignObject)喂进开场同款 WebGL 抖动、**c** 混合。

- **b 的技术验证是通过的**:截取保真度实测合格(内容/坐标/像素字体全部正确还原,字体内联生效),且因为是访客端实时截取,截图不会过时。
- **但 b 有 a 完全没有的结构耦合**:硬编码了"只抓 style.css / 只处理 1 个 @import / 1 个字体 / 不内联 CSS 背景图 / 只克隆 header.hud+main"五处假设,站点结构一改就坏,**且失败时静默回退到 a,坏了看不见**。
- **用户实测三者视觉差异看不出**,且明确后续要持续迭代网站与开场 → **按可维护性定案选 a**,b/c 代码全部删除。原则:视觉收益看不出来时,耦合就是纯负债。

- **顺带加固**:推镜视频的 `ended` 事件是主推进路径,但视频卡死/解码失败/被播放策略拦下时它永不触发,访客会僵死在静止画面上(求职站等于崩盘)。加了看门狗兜底(`armPushWatchdog`),超时强制推进;后台标签页视频本就暂停,那段时间不计入超时。已用"play() 必然失败"实测验证救场成功。
- **开机自检文案是虚构机器设定**(LIMU-OS/640K/RPG_SAVE.DAT),不是简历数据,不受诚实红线约束但也不得写成绩效数字;intro 文案(含 JS 里的 bootLines)纳入字体子集扫描源(`fonts/make-subset.py` 的 SOURCES)。
